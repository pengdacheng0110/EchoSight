# -*- coding: utf-8 -*-
"""盲人识物助手（桌面版入口）

流程：语音询问目标 -> 录音识别确认 -> 摄像头扫描(中文标注/测距/方位)
      -> 语音播报；找不到时提示移动手机。

全程语音控制，无需按键：
  说"找到了"        -> 停止播报，安静待命
  说"找XX / 换XX"   -> 切换目标；待命时说即恢复扫描
按 Q 退出（仅正常退出时用得上；异常退出会语音说明原因）。

原先这里写的是"T/空格 = 语音切换目标"，那是按键版的遗留说明 ——
现在 T 和空格不做任何事，只有 Q 有响应。入口文件是用户最先打开的地方，
写着不存在的操作方式比不写更糟。
"""
from assistant.desktop import DesktopApp

if __name__ == "__main__":
    DesktopApp().run()
